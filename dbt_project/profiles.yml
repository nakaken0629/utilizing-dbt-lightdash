ecommerce_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5434
      user: dbt_user
      password: dbt_password
      dbname: dbt_warehouse
      schema: public
      threads: 4
      sslmode: disable
